# Baked into the Vite production build (vite build reads this automatically).
# The backend URL is public, not a secret. Override in Vercel env vars if it changes.
VITE_API_URL=https://automom-backend.onrender.com
